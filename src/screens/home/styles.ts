import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    flex: 1,
    padding: 15,
    marginTop: 30,
    backgroundColor: '#000',
    // alignItems: 'center',
    // justifyContent: 'center',
  },
  sectionContainer: {
    gap: 10,
  },
  header: {
    color: '#FFF',
    fontWeight: 'bold',
  },
});
